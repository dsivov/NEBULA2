HOW TO USE:

Download the weights by doing the following steps:
1. chmox a+x ./download.sh
2. ./download.sh

For training use:
`train.py`

For testing use: (make sure you update the weights)
`test.py`
and the testing sentences should be in annotated_sentences.txt file
The output will be in `Output_predictions.txt`